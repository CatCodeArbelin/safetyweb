# Startup

Run the Windows development startup:

```powershell
.\up.ps1
```

Run a safe local check without UI or service startup:

```powershell
.\scripts\dev-check.ps1
```

Run the container-safe check:

```powershell
docker compose up --build
```
